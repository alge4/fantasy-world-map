{
  "name": "world-map",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w @world-map/shared build && concurrently -n backend,viewer -c blue,green \"npm run -w @world-map/backend dev\" \"npm run -w @world-map/viewer dev\"",
    "build": "npm run -w @world-map/shared build && npm run -w @world-map/backend build && npm run -w @world-map/viewer build",
    "start": "npm run -w @world-map/backend start",
    "typecheck": "tsc -v && echo TypeScript OK"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.5.4"
  },
  "license": "MIT"
}


